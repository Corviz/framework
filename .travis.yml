language: php

php:
  - '7.0'
  - hhvm
  - nightly

before_script:
  - composer self-update
  - composer install --prefer-source

matrix:
  allow_failures:
    - php: nightly

sudo: false

cache:
  directories:
    - $HOME/.composer/cache